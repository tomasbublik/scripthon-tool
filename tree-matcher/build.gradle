version = rootProject.version

dependencies {
    compile project(":scripthon-compiler")
    compile project(":tree-analyzer")

    compile "org.slf4j:slf4j-api:1.7.2"

    testCompile "junit:junit:4.11"
    testCompile "org.assertj:assertj-core:1.7.1"
}